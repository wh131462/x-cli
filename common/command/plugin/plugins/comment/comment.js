import {rootPath} from "#common/utils/file/path.js";

/**
 * comment
 * @type {IPlugin}
 */
export const comment = {
    check: async () => true,
    install: () => {
        const resolve
    },
    uninstall: () => {}
};
